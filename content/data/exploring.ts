/* =============================================================================
 *  CURRENTLY EXPLORING — living snapshot of what I'm learning right now.
 *
 *  👉 THIS IS THE FILE TO EDIT. Update it every few weeks. Nothing else to touch:
 *     - edit the `currentlyExploring` list below (add / remove / reorder freely)
 *     - bump `exploringAsOf` to the current month
 *  The About card re-renders automatically. No component or styling changes needed.
 * ============================================================================= */

export const exploringAsOf = "June 2026";

export const currentlyExploring: string[] = [
  "PostgreSQL + pgvector", // SQL for relational + vector DB
  "RAG pipelines",
  "Machine Learning (CS229)",
  "Shipping agents: Docker → K8s",
  "Going deeper on Azure + AWS",
];
